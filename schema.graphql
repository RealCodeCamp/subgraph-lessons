type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  src: Bytes! # address
  guy: Bytes! # address
}

# Transfer(indexed address,indexed address,uint256)
type Transfer @entity {
  id: ID!
  by: Bytes! # address
  to: Bytes! # address
  amount: BigInt! # BigInt or BigDecimal
}